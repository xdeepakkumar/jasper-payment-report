spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update

  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/demo-jasper-report
    username: postgres
    password: admin
    driverClassName: org.postgresql.Driver
